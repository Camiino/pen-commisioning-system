#pragma once

// Compile against the vendored WiFiManager copy in ../libraries so the sketch
// can build in Arduino IDE without requiring a separate global library install.
#include "../libraries/WiFiManager/WiFiManager.h"
